# =============================================================================
# Examples 顶层 Makefile
# =============================================================================

# 子目录
obj-y += lib/
obj-y += tools/

# Ensure tools is built after lib (for parallel builds)
tools/: lib/
